El makefile esta dentro de la carpeta src

Las funciones implementadas fueron


Permutar: Lee el string y si la iteracion es par realiza el cambio.

Rot13: Lee el string caracter por caracter aplicando una rotacion de 13 en el abecedario. www.rot13.com

Pascal: Lee el string, lo separa en 2 arreglos, izq y der. Ordena los caracteres como se piden y luego antes de juntar ambos strings y el caracter central invierte el arreglo izq.

Backward: Lee el string y mueve los caracteres en casillas pares al principio del string.

Reverse: Invierte el string.

Tuve dificultades al implementar librerias dinamicas, el codigo sin ellas funciona de lo mas bien. En el estado actual da segfault